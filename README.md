# 换线清场与首件放行

本项目记录包装产线换产品时的清场证据、设备校验和首件结果。检查范围由产品转换关系确定，区域证据、人员签字和机器回执都只能追加。

`domain_contract.json` 描述换线、检查项和开线令牌状态。离线终端消息带设备内序号，令牌始终绑定产线、产品、配方版本和有效期。

运行基础检查：

```bash
python -m unittest discover -s tests -v
```

